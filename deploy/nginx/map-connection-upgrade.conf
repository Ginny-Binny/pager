# Required by the ntfy vhost's WebSocket upgrade. Belongs in the http{} block,
# so it goes in conf.d/ rather than sites-available/.
map $http_upgrade $connection_upgrade {
    default upgrade;
    ''      close;
}
