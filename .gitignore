# Contains NTFY_TOKEN and the /status password hash.
.env

# Build output
/pager
/pager.exe
/out/

# Claude Code settings are per-developer, not project policy. A committed
# settings.json silently pre-approves tool permissions for everyone who clones
# the repo, which is not something a repo should decide on your behalf.
.claude/
